// next.config.mjs
// Clean config — no alias to '@vercel/kv'.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true
};

export default nextConfig;
