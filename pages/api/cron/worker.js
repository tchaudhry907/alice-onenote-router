// pages/api/cron/worker.js
// Temporary stub to avoid build-time imports until cron is needed.

export default async function handler(req, res) {
  res.status(200).json({ ok: true, cron: 'disabled' });
}
